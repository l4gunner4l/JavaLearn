1) Что обязательно должна иметь переменная при объявлении?
- Тип, имя и значение
- Имя и значение
+ Тип и имя
- Достаточно только имени

2) Каким образом объявляются переменные?
+ тип_данных имя_переменной
- имя_переменной тип_данных

3) Что происходит при инициализации переменной?
- Переменной присваивается тип
- Переменная получает имя
+ Переменной присваивается значение

4) Какое имя для переменной верное?
- 1name
- int
+ userName1
- w=1

5) Чем отличается инициализация от объявления переменной?
- Это одно и то же
- При объявлении переменная получает значение, а при инициализации переменной необязательно присваивается значение
+ При инициализации переменная получает значение, а при объявлении переменной необязательно присваивается значение
- При инициализации переменная получает название, а при объявлении - значение

6) Что выведет программа?<br/>
<tt>int x = 0;<br/>
System.out.println(x);</tt>
+ 0
- null
- x
- В программе ошибка

7) Что выведет программа?<br/>
<tt>int y;<br/>
System.out.println(y);</tt>
- 0
- null
- y
+ В программе ошибка

8) Что такое final в Java?
- Тип данных
- Постоянная переменная
+ Ключевое слово для объявления константы
- final не существует в Java

9) Что выведет программа?<br/>
<tt>final int LIMIT = 5;<br/>
System.out.println( LIMIT );<br/>
LIMIT = 57;<br/>
System.out.println( LIMIT );</tt>
- LIMIT
- final
- 57
- 5
+ В программе ошибка

10) Что выведет программа?<br/>
<tt>int x, y = 5;<br/>
x = 10;<br/>
System.out.println( x + y );
- 10
+ 15
- 0
- 20