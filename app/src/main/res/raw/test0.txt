0) Что обязательно должна иметь переменная при объявлении?
- Тип, имя и значение
- Имя и значение
+ Тип и имя
- Достаточно только имени

1) Каким образом объявляются переменные?
+ тип_данных имя_переменной
- имя_переменной тип_данных

2) Что происходит при инициализации переменной?
- Переменной присваивается тип
- Переменная получает имя
+ Переменной присваивается значение

3) Какое имя для переменной верное?
- 1userName
- int
+ userName1
- user&name

4) Чем отличается инициализация от объявления переменной?
+ При инициализации переменная получает значение, а при объявлении переменной необязательно присваивается значение
- При объявлении переменная получает значение, а при инициализации переменной необязательно присваивается значение
- При инициализации переменная получает название, а при объявлении - значение
- Это одно и то же

5) Что выведет программа?
    int x = 0;<br/>System.out.println(x);
+ 0
- null
- x
- В программе ошибка

6) Что выведет программа?
    int y;<br/>System.out.println(y);
- 0
- null
- y
+ В программе ошибка

7) Что такое final в Java?
- Тип данных
- Постоянная переменная
+ Ключевое слово для объявления константы
- final не существует в Java

8) Что выведет программа?
    final int LIMIT = 5;<br/>LIMIT = 57;<br/>System.out.println( LIMIT );
- LIMIT
- final
- 57
- 5
+ В программе ошибка

9) Что выведет программа?
    int x, y = 5;<br/>x = 10;<br/>System.out.println( x + y );
- 10
+ 15
- 0
- 20